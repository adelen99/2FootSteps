To launch the server, first download XAMPP and start both Apache and MySQL. Once that's done, you can run the MySQL script.

For the frontend, run the commands npm install to install dependencies and npm run dev to start the development server.